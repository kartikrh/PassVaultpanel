services:
  panel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: panel
    restart: always
    ports:
      - "4001:3000"
    environment:
      - NODE_ENV=production
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
